# 어노테이션이란?

어노테이션(Annotation, @)은 사전적인 의미로는 주석이라는 뜻이다.

자바에서 코드 사이에 주석처럼 쓰이며 특별한 의미, 기능을 수행하도록 하는 기술이다.

즉, 프로그램에게 추가적인 정보를 제공해주는 메타데이터 라고 볼 수 있다.

# 특징

- 컴파일러에게 코드 작성 문법 에러를 체크하도록 정보를 제공
- 소프트웨어 개발 툴이 빌드나 배치시 코드를 자동으로 생성할 수 있도록 정보를 제공
- 실행시(런타임) 특정 기능을 실행하도록 정보를순 제공

## 어노테이션 사용 순서

- 어노테이션 정의
- 클래스에 어노테이션 배치
- 코드가 실행되는 중에 Reflection을 이용하여 추가 정보를 획득하여 기능을 실시

# 알아두면 좋은 어노테이션 종류

Spring Boot에서 사용되는 어노테이션의 종류는 다양하다. 아래는 개발자들이 자주

접하게 되는 어노테이션들에게 대한 정보이다.

### 🔹 기본 구성

| 어노테이션 | 설명 |
| --- | --- |
| `@SpringBootApplication` | 애플리케이션의 시작점 |
| `@Component` | 스프링이 관리할 객체(빈) 등록 |
| `@Bean` | 자바 코드로 직접 객체 등록 |

---

### 🔹 계층 구조 관련

| 어노테이션 | 설명 |
| --- | --- |
| `@Controller` | 웹 MVC 컨트롤러 |
| `@RestController` | REST API 전용 컨트롤러 (`@Controller + @ResponseBody`) |
| `@Service` | 비즈니스 로직 담당 클래스 |
| `@Repository` | DB 접근 로직 클래스 (예외 변환 기능 포함) |

---

### 🔹 웹 API 관련

| 어노테이션 | 설명 |
| --- | --- |
| `@GetMapping`, `@PostMapping` | HTTP 요청을 처리하는 메서드 지정 |
| `@RequestParam` | 쿼리 파라미터 값 받기 |
| `@PathVariable` | URL 경로 변수 받기 |
| `@RequestBody` | JSON 요청 본문을 객체로 매핑 |
| `@ResponseBody` | 객체를 JSON으로 변환해 응답 |

---

### 🔹 DI (의존성 주입)

| 어노테이션 | 설명 |
| --- | --- |
| `@Autowired` | 자동으로 객체를 주입 |
| `@Qualifier` | 같은 타입의 빈이 여러 개일 때 구분 |
| `@Value` | 설정 값 주입 (환경변수, yml 등) |

---

### 🔹 예외 & 검증

| 어노테이션 | 설명 |
| --- | --- |
| `@Valid` | DTO 유효성 검증 트리거 |
| `@NotNull`, `@Size`, `@Email` | DTO 필드 조건 지정 |
| `@ExceptionHandler` | 예외를 잡아 커스텀 응답 처리 |
| `@ControllerAdvice` | 전역 예외 처리 클래스 등록 |

---

### 🔹 데이터베이스 (JPA)

| 어노테이션 | 설명 |
| --- | --- |
| `@Entity` | DB 테이블과 매핑되는 클래스 |
| `@Id` | 기본 키 지정 |
| `@GeneratedValue` | 기본 키 자동 생성 전략 |
| `@OneToMany`, `@ManyToOne` | 연관 관계 매핑 |

# 📁 기본 구성

## `@SpringBootApplication`

Auto-Configuration, Component-Scan 등을 통해 하위에 있는 패키지의 모든 설정

과 생성을 자동으로 하는 어노테이션. 프로젝트 최상단에 선언해야 한다.

```java
@SpringBootApplication
public class Application {
public static void main(String[] args) {
SpringApplication.run(Application.class, args);
	}
}
```

main 메소드 안에 있는 SringApplication.run을 통해 내장되어 있는 톰캣이 자동으로

실행이 되어 별도의 WAS 설치가 필요 없다.

## **`@Component`**

컴포넌트 스캔을 통해서 감지되어 자동으로 Bean이 등록될 후보 클래스를 명시한다.

Class 단위에 붙는 어노테이션으로, @Controller , @Configuration  등 전부 내부

에 @Component 어노테이션을 가지고 있어, 컴포넌트 스캔의 대상이 된다.

## `@Bean`

Bean을 등록하는 어노테이션. 스프링 컨테이너에 직접 객체(Bean)를 등록할 때 사용

하는 어노테이션이다. 다른 어노테이션과 다르게 메서드에 붙여서 Bean을 등록한다.

```java
@Bean
public MemberService memberService(MemberRepository memberRepository){
    return new MemberServiceImpl(memberRepository());
}

@Bean(name="jdbcMemberRepository")
public MemberRepository memberRepository(){
    return new JDBCMemberRepository();
}

@Bean
public MemberRepository jpaMemberRepository(){
    return new JpaMemberRepository();
}
```

Bean은 기본적으론 메서드의 이름을 camel case 로 변경한 id로 등록이 되지만

name을 입력하면 그 값으로 등록된다.

# 📁 계층 구조 관련

## `@Controller`

Spring MVC의 컨트롤러인 @Controller는 주로 View를 반환하기 위해 사용한다.

![[Pasted image 20250922094527.png]]

이미지와 같이 Spring MVC Container는 Client의 요청으로부터 View를 반환한다.

클라이언트는 [[URI]] 형식으로 웹 서비스에 요청을 보냄 → DispatchServlet에서 요청을

처리할 대상을 찾은 후 HandlerAdapter을 통해 요청을 Controller로 위임한다.

Controller가 요청을 처리한 후 View Name을 반환하며 DispatcherServlet은 View

Resolver를 통해 View Name에 해당하는 View를 찾아 사용자에게 반환한다.

```java
@Controller
@RequiredArgsConstructor
public class UserController {

    private final UserService userService;

    @GetMapping(value = "/users")
    public @ResponseBody ResponseEntity<User> findUser(@RequestParam("userName") String userName){
        return ResponseEntity.ok(userService.findUser(user));
    }
    
    @GetMapping(value = "/users/detailView")
    public String detailView(Model model, @RequestParam("userName") String userName){
        User user = userService.findUser(userName);
        model.addAttribute("user", user);
        return "/users/detailView";
    }
}
```

## `@RestController`

JSON을 반환하는 컨트롤러로 만드는 어노테이션이다. 주로 REST API를 개발할 때 사

용하며, 객체를 ResponseEntity로 감싸서 반환한다. 동작 과정은 @Controller에

@ResponseBody를 붙인것과 완벽하게 동일하다.

## `@GetMapping / @PostMapping / @PutMapping / @DeleteMapping`

Http Method의 Get, Post, Put, Delete의 요청을 수행할 수 있게 하는 어노테이션. REST API 규칙의 따라 Get / Post / Put / Delete 기능에 맞게 변경해서 사용 가능하다.

```
@GetMapping(path = "/hello")
	public String getHello() {
    	return "Hello Spring!";
	}
```

```
@PostMapping("/path-variable/{name}")
    public String pathVariable(@PathVariable String name) {
       return name;
}
```

```
@RestController
@RequestMapping("/api/put")
public class PutController { }
```

```
@RestController
@RequestMapping("/api/delete")
public class DeleteController { }
```

https://sddev.tistory.com/225

https://velog.io/@kgh2120/Spring%EC%9D%98-%EC%9C%A0%EC%9A%A9%ED%95%9C-%EC%96%B4%EB%85%B8%ED%85%8C%EC%9D%B4%EC%85%98-%EC%A0%95%EB%A6%AC

https://iliveinseoul.tistory.com/entry/SpringBoot-Annotation-%EC%82%AC%EC%9A%A9-%EC%A0%95%EB%A6%AC

---

https://velog.io/@hunzz/spring-boot-get

https://velog.io/@hunzz/spring-boot-post

https://velog.io/@hunzz/spring-boot-put-delete